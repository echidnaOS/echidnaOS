#include <stdio.h>

void help_cmd(void) {
    puts(
        "echidnaOS built-in shell.\n"
        "Available commands:\n"
        "\n"
        "clear - clears the screen.\n"
        "help - prints this guide."
    );
    return;
}
